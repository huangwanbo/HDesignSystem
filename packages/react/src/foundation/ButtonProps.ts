const buttonTypes = Object.freeze({
  primary: "primary",
  success: "success",
  error: "error",
  warning: "warning",
});
const buttonSizes = Object.freeze({
    sm: "sm",
    base: "base",
    lg: "lg"
  });
const htmlType = Object.freeze({
    submit: "submit",
    button: "button",
    reset: "reset"
})
export {
    buttonTypes,
    buttonSizes,
    htmlType
} 
