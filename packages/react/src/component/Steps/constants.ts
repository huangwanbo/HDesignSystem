export const type = Object.freeze({
    default: 'object',
    arrow: 'arrow',
    dot: 'dot',
    navigation: 'navigation'
})

export const sizeType = Object.freeze({
    default: 'default',
    small: 'small',
})

export const directionType = Object.freeze({
    vertical: 'vertical',
    horizontal: 'horizontal'
})

export const labelPlacementType = Object.freeze({
    /* 放图标右侧 */
    horizontal: 'horizontal',
    /* 放图标下面 */
    vertical: 'vertical'
})

export const statusType = Object.freeze({
    wait: 'wait',
    process: 'process',
    finish: 'finish',
    errors: 'errors',

})